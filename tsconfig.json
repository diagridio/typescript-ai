{
  // Solution-style root config: it owns no files itself, it only wires the
  // referenced projects together so `tsc --build` type-checks the whole
  // workspace in dependency order (core -> mastra/n8n -> examples -> tests).
  "files": [],
  "references": [
    { "path": "./packages/core" },
    { "path": "./packages/mastra" },
    { "path": "./packages/n8n" },
    { "path": "./examples/mastra" },
    { "path": "./examples/n8n" },
    { "path": "./tests" }
  ]
}
